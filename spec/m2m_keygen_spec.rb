# frozen_string_literal: true

RSpec.describe M2mKeygen do
  it "has a version number" do
    expect(M2mKeygen::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
